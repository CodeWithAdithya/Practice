/* Write a program to print Minimum Element in an Array */

import java.util.Arrays;

public class JavaConqueror
{
    public static void main(String[] args)
    {
        int[] nums = {1,5,3,7,8,23,67,432,4};
        System.out.println("Minimum Value is " + minimumElement(nums));
    }
    static int minimumElement(int[] nums)
    {
        if (nums.length==0 || nums==null)
        {
            return -1;
        }
        int minNum = Integer.MAX_VALUE;
        for (int i = 0; i < nums.length; i++)
        {
         if (nums[i]<minNum)
         {
             minNum=nums[i];
         }
        }
        return minNum;
    }
}

Output:
Minimum Value is 1
